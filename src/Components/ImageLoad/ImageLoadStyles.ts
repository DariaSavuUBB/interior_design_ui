import { mergeStyles } from "@fluentui/merge-styles";

export const loadImageClassName =  mergeStyles({
    width: '20%',
    height:'95%',
    padding:'2vh 2vw',
    border:'2px solid black'
});

export const imageContainerClassName = mergeStyles({
    width:'100%',
    height: '80%'
});